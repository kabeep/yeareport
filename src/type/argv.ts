export type Argv = {
    author?: string;
    since?: string;
    before?: string;
    lunar?: boolean;
    output?: string;
    reverse?: boolean;
    overwrite?: boolean;
    pretty?: boolean;
    appendType?: string[];
};
